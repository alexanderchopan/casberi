# Handoff — Casberi

## Opening line for the next session
"Continue Casberi. Read session-handoff-casberi.md, build-brief.md, prd.md."

## State
Definition closed. Name: Casberi (ledger holds the checks; exposure begins at the store listing). Framework runs 1 (Bob) and 2 (Alice) complete — one problem set at two rungs, one feature set at two loads, zero surfaces added for Alice. PRD at draft 3 with every delta folded. Build brief written. Prototype v94 = spec. Next act: the build, in Claude Code.

## Files (all in outputs)
- **build-brief.md** — the builder's document. Stack, data model, server API, gen UI port, write model, screens + derivations, tokens, gap list, milestones M0–M7, rules.
- **prd.md** — the why. Thesis, personas + operate variable, evidence ×16, P1–P8, S1–S11 with the Bob/Alice table, run-2 synthesis + risks, all surface specs, design principles.
- **name-ledger.md** — the pick, the rules, every cut with reasons. Runners-up: Cachall (blocked pending CATCHALL #97442227 status), Cachel, Caberi, Cachup.
- **composer-rest-chips-v94.html / .tsx** — visual spec. Drop the phone frame; it is demo chrome.

## Decisions this session
1. Run 2 (Alice) completed on paper. Operate named as the persona variable — six goals fixed, operate budget varies (Bob zero, Alice large). P8 added: "isn't aware" — push, where P4 is pull. S7 killed (native superseded; intent lives in S4). S10 (approvals carry context) and S11 (state lands as things) added. Seed pool (Workboard, Skill Workshop, Instances) closed at zero surfaces. Names hold: target = Bob, the consumer.
2. Write model reworked for native: rung 2 = hand-off writes through App Intents; the Apple half of rung 3 merged in. "Open shortcut" dies.
3. Thing sheet spec'd: header morph, content by kind, verbs card (verbs derive from kind × source × bridge state; cap three; menus die), mark control, one Tags field, Related shelf streams last. S10 approval sheet = this sheet, post-v1.
4. Name: Casberi. kaz-BEHR-ee. cache + berry merged past the cash sound; casa in the front, berry in the tail. Locked without counsel.

## Rules that keep deciding
The sound is the bar. Verbs derive; menus die. The record paints; synthesis streams. Reads pass, writes confirm. Every value routes through a token. One signature moment per screen. Settings hold nothing. Tolerance is not desire — the product minimizes operate at every budget.

## Kickoff prompt for Claude Code
"Read build-brief.md and prd.md in this directory. Brief §8 is the design system; treat it as law — every value routes through the token layer, components hold zero raw hex. composer-rest-chips-v94.html is the visual spec — open it, match its grammar, drop the phone frame. Scaffold M0: Xcode project (SwiftUI, SwiftData, iOS 18 target), the token layer per §8, the four-tab glass shell with composer, and a demo corpus of 30 things across kinds. Stop after M0 for review."

## Parallel track
Apple Developer Program enrollment ($99/yr). M0–M4 build in the simulator without it; TestFlight at M5 needs it.

## Known gaps (in the brief, §9)
Topic blocks → project detail. Widget child-drop. Light-mode photo legibility. Mic capture unwired. Hero morph → matchedGeometryEffect. Real app icons.

## Queue
1. M0 scaffold + review.
2. M1 capture paths.
3. Name screen before store listing (option, not gate).
4. S11 Bob rung: context signals — parked until the corpus exists.
