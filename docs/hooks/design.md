# Design — the long form

The dock, the room chassis and the design-law entries, verbatim as they stood in CLAUDE.md. CLAUDE.md keeps one line each.

## Moved from CLAUDE.md (2026-09-14)

Verbatim. CLAUDE.md now carries a one-line index entry for each, pointing here.

- **The dock is FLAT CHIPS ON ONE GLASS SLAB, and its scrub is SwiftUI's own sequence (2026-09-09).** Eleven `.interactive()` glass chips in a `GlassEffectContainer` inside the slab's glass inside a scroll forced the magnification to be LAYOUT (resizing the scroll's content under a running flick) and the melt to be a viewport MASK; both are a transform and a `visualEffect` now. The wave rides the SCRUB and the POINTER only — under a plain scroll the strip moves WITH the finger, so nothing ever swept. `DockScrubCatcher` (a UIKit recognizer bolted to SwiftUI's private hosting scroll view) is DELETED: it **had never once fired on the user's phone** while three sessions guarded and tuned it. NSLogs `dockScrub:` in DEBUG — check it on a device** → prd §660. **AMENDED THE SAME DAY (prd §662g): the sequenced press FROZE THE STRIP'S SCROLL DEAD — build 543 shipped a dock that could not be scrolled by a finger, bisected on the simulator. A SwiftUI drag on scroll content, sequenced or simultaneous, is the standing gotcha and is measured three times now. The hold is `DockHold`, a `UILongPressGestureRecognizer` through `UIGestureRecognizerRepresentable`, and `dock-selftest.sh` refuses any SwiftUI drag or long press inside the strip. THEN DELETED OUTRIGHT (prd §662h, same evening, user: "w/o a mouse like on a mac who is going to really scrub?" → "kill it"): a hidden hold-then-slide on a screen with no pointer. The wave stays for the POINTER on iPad/Mac; nothing in the strip claims a touch**
- **A category tap LANDS FIRST, then springs its folder (prd §668, 2026-09-10).** Three animations in one frame was the reported stutter; the folder waits `flightMs + 80` (past `deal`'s own landing, generation-guarded). `DockFolderRow` resolves broken seats ONCE per body, its deal staggers six seats, and `BridgeIcon` memoises its asset name and image (64 call sites) → prd §668
- **"Still not smooth" (prd §673, 2026-09-10): `.transaction { $0.animation = … }` rewrites EVERY transaction reaching a shape, the fold's un-animated per-frame write included — guard on `t.animation != nil`. The strip's body reads no `BridgeStore` (two leaves do); `reconnect` writes once, only on change. UNCOMPILED — built on a Linux session** → prd §673
- **A CROWN'S LINE PAYS FOR ITS OWN RANGE CHIPS, and the budget is ONE expression (prd §720, 2026-09-14, user: "7d and watched is clipping").** `DSRoomSlot` clips, so a crown whose line takes the whole box pushes its own `7d | Watched` track out through the bottom edge. §688 measured the track (`crownRangeChips` = 42) and spent it in `RoomHomeCrown` only — and the WALLET room does not go through that template, nor does `RoomActivityChart`, so both went on budgeting the chips at nothing. `DSRoomChassis.crownChart(box:chips:)` is the one expression now; it ASKS, because a record with one honest window draws no chips (§83). **The class: a fix applied to a shared template reaches only what goes through it — check what still draws that shape by hand** → prd §720 · §688
- **A chip tap LANDS in the category's room and springs its folder; a swipe walks INDIVIDUAL ROOMS in the dock's order, crossing category edges (2026-09-09).** The 2026-09-06 "a folder tap doesn't switch what is on your screen" is overturned for the row (kept for the chip you are standing in, which toggles); `neighbour(_:)` expands each category to `CategoryFold.scopes` → prd §663
- **A category chip is a TILE — a glyph over its word at a fixed 52pt, `dockCaption10` under a frozen 20pt SF Symbol from `CategoryFold.glyph(for:)`, one `chipShape` for every ring and hit region, one bounce on landing (2026-09-09).** The capsule's pitch was set by its longest name; five chips show before a scroll where three and a half did. A new category needs a row in `CategoryFold.glyphs` or the fold self-test fails → prd §662
- **The page is NOT clipped at rest and the bottom band paints NO plate (prd §677, 2026-09-10).** `PagerDrag`'s clip is `PageClip` — a Shape reaching below the page at rest, the rounded card only while lifted — because a `clipShape` with radius 0 still clips to bounds that stop at the band, slicing every row beneath the dock (the "black bar"). §649's opaque plate and the bottom scroll-edge fade are retired with it; rows run under the folder and the dock and the glass refracts them. `dock-selftest.sh` pins all three → prd §677
- **The dock's FOLDER touches the dock; a FACE RAIL sits above it (2026-09-08). `DockSpringRow`'s tail points DOWN at the chip it sprang from — under the old order it aimed at the avatars. The band's scrim ramps over a FIXED 24pt, never a fraction of its own height (a 3-row band was 54pt of see-through)** → prd §649
- Liquid Glass on the floating layer only (composer/FAB/toasts) — never on content. (The tab bar was dropped in `0764ee3` (2026-07-13): one surface now, a source-chip header + a FAB, no tabs. Ruling: prd §100 (recorded 2026-07-17). Older PRD rulings — §16, the store/onboarding batches, §61's amended elevation-law example — still narrate a tab bar; those are historical ledger entries, true when written, deliberately NOT rewritten; read them against §100. The chip header itself led with a "Pinned" chip/board until that retired 2026-07-20 (prd §131) — it now leads with "All"; the FAB is the agent's own rise/lower bar, not the old board's.)
- No hairlines — zero exceptions (2026-07-10: the Apps page's CONNECTED strip and its divider died; the catalog is ONE surface where a connected app wears its state and opens management). Nothing draws a line. Widget/tile radius = `DS.Radius.widget`. **That surface is a LIST since prd §518 (2026-08-29), not a grid** — sectioned by category, filtered by a `DSSectionSwitcher` strip whose first chip is All, one `appRow` per app. The wall (§200/§201) and its `appTile` are deleted, and the reason is worth carrying: the wall wore the SOURCES TRAY's own grammar (a grid of brand tiles grouped by the same `BridgeCatalog.categories`), so the two screens differed in content and in nothing a person reads first — user, 2026-08-29: *"the app catalogue in current form competes w the Your feeds tray and is confusing which is which"*. The split is now structural and must stay that way: **the tray keeps tiles and means "the sources you already have"; the catalog is rows and means "what you could add"**. A future pass that gives the catalog tiles back re-creates the collision.
- **EVERY POUR IS INK — `DS.pourInk`, one token, no exceptions (prd §524, 2026-08-29).** A card, a sheet head or a page gets a top the same way everywhere: `pourInk` → clear over 150pt (a page's own washes run longer), alpha baked into the adaptive colour so no view reads `colorScheme` for it. **`dsReceiptPaper` does not TAKE a colour** — the `Color?` whose nil meant "no pour" is gone, because a neutral makes no claim to opt out of and a paper without a top is the "jumble of text" §495 exists to fix. The rule that decides any new case: colour that says WHAT IS HAPPENING stays (the amount's tone, a stamp's ink, a mode disc, the connect bloom, the address face's reveal); colour that says WHERE IT CAME FROM goes, because the mark directly above the pour already says it at full strength. `DS.receiptPour`'s nine hues and `MoneyReceipt.Hue` still exist and are still harnessed — `DSStamp.shielded` is the one live caller, a word rather than a wash — so nothing here needs re-deriving if this is ever reversed. **UNSEEN on a device**: 0.078 was chosen against a mockup at the real tokens, so check it reads as a top rather than a band before trusting it.
- Apps Browse categories follow prd §59 (2026-07-11): X browses under **Social** (with Bluesky, Farcaster, Telegram) — a social account first, not a saves source; Slack browses under **Work** (with GitHub, Linear, Notion) — a workplace tool, not a messenger; and **Notes** is its own category (Apple Notes, Day One, Apple Journal, Obsidian) — the vault is notes, not project tracking. Taxonomy lives in `Model/BridgeCatalog.swift`; website Browse sections mirror it in the same session.
- Honesty rule: no dead controls, no fake status. Three corollaries the 2026-07-16 audit paid for (prd §83): a hand-rolled button that paints its own background MUST swap that background when disabled — `.disabled` dims a plain-style button's label, not your background, so it reads live while inert (prefer `BridgeFieldRow`); never quote a price off a stale last trade — quote the live book's own bracket midpoint, or show nothing (an empty bid-0/ask-1 book is the one bracket that says nothing; its midpoint would invent a 50% coin flip), and read both sides of a delta the same way; and a change that rounds to zero has no direction — no sign, no green/red (`TokenChartStyle.isFlat` / `accent(change:)`). The iCloud-sync ship gate is MET (2026-07-07): CloudKit capability is in the build, voice audio rides the store (externalStorage → CKAsset), Delete everything purges the CloudKit zone, and only the app process mirrors (extensions use `SharedStore.extensionContainer()`). "End-to-end encrypted" claims still require Advanced Data Protection — don't overclaim.
- **THE MAC TAKES ITS OWN POINT SCALE — `DSTextStyle.macScale = 0.88`, one lever, no per-rung tuning (prd §631, 2026-09-06).** `TARGETED_DEVICE_FAMILY` includes family 6, so a point here is an AppKit point at 1:1 and the app's 17pt reading band rendered at a true 17 beside a system whose list text is 13. The ramp and `dsGlyph` both take the factor (a glyph holding its size while its label shrank breaks §359's icon↔label pairing); `DS.Face`/`DS.Mark` deliberately do NOT (a face is a picture and often a 44pt tap target, not type). The eleven `widget*` rungs opt out — a WidgetKit tile is the same size on both platforms — enforced as check 5 of `design-ramp-audit.py`. Safe unseen only because the direction is forgiving: smaller text inside a pinned frame leaves air, bigger text cuts. **UNSEEN on a Mac.**
- **The Mac's walked row can be TAKEN: ⌘C, Space (Quick Look), and drag-out (prd §631, `Shell/MacRowHandoff.swift`).** One resolver behind all three — the URL comes from `ShareTargetMemo`, so a row cannot copy one link and share another. ⌘C is safe because every `textSelection(.enabled)` site is in a sheet, a pushed room or the composer, all of which set `canWalk` false (measured, not assumed). Space is enabled only for screenshots and files, so on a link row it stays the scroll key. Drag is Mac-only on purpose: `.draggable` on touch IS the long-press the context menu already owns, and the pager claims every horizontal drag (2026-07-16). `KeyboardWalk` split into the RING and `WalkRowVerbs` because nine observers on one chain exceeded the type checker.

- **A row that names a place is a BUTTON, or it is deleted, prd §736, 2026-09-15 (user: "can we apply that same principle to all the things that say from something / why even have them say from, why not just have an open button").** Asked the day after §735 made the thing sheet's "From — in your inbox" row a door, and it is the right question about that ruling. **The finding is that the open button already existed**: `VerbDerivation` gives a `.event` "Open in Calendar", a Files `.file` "Show in Files", a social `.chat` "Open thread", a `.transaction` "Explorer", a `.product` "Open in store", a `.screenshot` "Zoom" and "Open in Photos", an Obsidian `.note` "Open in Obsidian", a Reminders row its hand-off — and, since §735, a `.mail` "Open in Mail"/"Open in Gmail". So the row was never a door waiting to be wired; it was the same fact a second time, stated weaker, sitting above the disc that already did it. **§634 had set the test and stopped halfway.** It deleted four `PlaceWords` arms for saying something true of every row in the corpus and kept the rest on the grounds that they "name a real place" — but "in your photos" is true of every screenshot, "in your contacts" of every contact, "in your home" of every accessory, "from your machines" of every run; "awaiting your call" is a state wearing the label "From"; and `.mail` and `.file` shared one arm, so **every Dropbox file in the corpus read "in your inbox"**, which is not thin but false (the Files carve-out for §408 went in above it and nobody re-read the arm underneath). **Two arms passed** — "in Receipts" and "in Main", the only two that said WHICH one — **and both are now the word on a button that goes there**, following `walletVerbs`' 2026-08-04 ruling that a disc's glyph says it opens something and its word says where you land: `Show in Receipts` (from `FilesLocation.folderName` off the ref the verb already parses, plus `FilesStore.shared.folderName` — an in-memory string, the `ObsidianStore.shared.vaultName` precedent, never the `folderURL()` disk read that arm's own comment refuses), and the wallet's own name on a new **`Verb.Action.openAddress`** — the only destination in that enum that never leaves the app, because the place a wallet row comes from is a screen Casberi draws. `WalletStore.displayName(forStored:)` is the word and the gate both (it carries the ENS-vs-hex matching and answers nil for an address that is not a watched wallet, so a stranger's transaction grows no disc); `isAutoName` decides between "Main" and "Wallet …4f4f". The stage sheets have had that door since §369 via the receipt's subject face — a mint, a card spend or a DeFi move gets no stage, so it held the fact and had nowhere to go, which is §408's shape one kind over. **`dialLabel` gains `"Show in "`**: without it "Show in Receipts" is 16 characters, falls past the 12-char gate to `shortLabel`, and reads "Files" — the whole ruling undone by a length check. **Deleted from the MODEL, not just the surface (§723)**: `PlaceWords`, `walletPlace`, `hasFrom` and `fromRow` are all gone, and nothing composes a place phrase anywhere now. The spec table holds Landed, Site, Also saved from, By, Who — every one a fact about this thing. **Four harnesses inverted with it.** `social-sheet`, `agent-sheet` and `purchase-stage` each guarded a stand-down (§363, §367, §364 — four passes each adding a conjunct saying "not on my sheet either"); §736 read that as the answer rather than the fifth conjunct, so each now guards that the row stays deleted, read from a COMMENT-STRIPPED copy because the view carries a tombstone naming what went (the Obsidian/Cursor lesson). **§408 and §735 are absorbed, not reversed**: both were right that people press that row — twice, two different moments, which is evidence — and they press it because it makes a claim about a place. The claim now lives on the control that honours it. **UNBUILT: authored on Linux with no Xcode and no Swift toolchain.**

## The All feed's day seam and the corpus floor (prd §866, §866a)

`FeedSeam` ticks once as a day divider crosses the top of the feed's viewport.
Four guards, each of which reads like a line a later pass could delete:

- **Finger down only** — `ScrollPhase.tracking`/`.interacting`, taken in
  `minimizesChrome`'s existing phase observer. A fling crosses six days in half
  a second, and the deceleration after you let go is silent on purpose: the days
  going by then are the phone's doing, not yours.
- **First placement is not a crossing.** Without `was != 0` every divider the
  `List` mounts ticks — landing on the feed buzzes, and so does any scroll that
  brings a new day into the window. This is the guard that separates a texture
  from a rattle.
- **A 300ms gap**, doing velocity's job without measuring velocity. Two seams
  inside it were ONE motion; the second is dropped, never queued.
- **A ±8pt dead band**, because the line is a place a divider can park and
  sub-point jitter would read as a crossing every frame.

It rides `FeedDayDivider`, whose only caller is `bundledSections`, so it is
All-only by construction rather than by a flag. `DSHaptic.seam` is the one verb
in the grammar naming something the person did not DO, so it is the lightest
feel — under `pour`, which is at least an event. It is deliberately NOT in
`GestureGate` (§666), whose every flag feeds `busy` and `HitchMeter`.

`CorpusFloor` keeps §218's line and strokes `CasberiMarkShape` on above it
before settling into the solid mark. That shape was built for §5's draw-on in
2026-07-06 and had sat at ZERO callers since that screen was cut — **grep for
the shape before building a motion**. Its silhouette also had to move from the
three-arm small cut to `armsFull`: the floor draws at `DS.Mark.tile` (44), well
past `smallCutBelow`, so a three-armed outline was settling into a five-armed
mark and growing two limbs at the crossfade.

**§866a — it stands down while `Show older` is up.** §264 windows the feed at 30
drawn rows with a door to the rest, and this section renders BELOW that door, so
the feed read `Show older`, then the mark, then "This is where it starts ·
Jul 23" with 122 more days behind the button above it. The date comes from
`visible` — the whole corpus — so it was never the oldest thing on screen. The
rule already existed one control away: `olderRow`'s own doc says
`caughtUpFooter` stands down while it shows, for §83's fake-status reason.
"You're all caught up" and "this is where it starts" are the same claim at
opposite ends of one list, and only one was gated. **When a guard exists for one
end of a range, ask what the other end says.**

- **THE SWIPE IS DEALT ON THE BRAND GROUND (prd §898, 2026-09-23 — user: "what if that background was pink … it would need to make the entire background pink").** The ground under a room swipe was black: the next room's cover (its last look under a 45% wash, or the bare page) sliding in beside a black card on a black shell, so §648's whole card vocabulary hung on a 1pt lit edge. Now `SwipeGround` sits on the SHELL's coat (`MainSurface`'s `.background`, under the status bar and the band — inside the pager it stopped at the pager's frame, the first mock's "you have black in the background still") and paints `DS.brandGround` at `min(1, abs(pageDragProgress))`: full at the turn, held through the flight, faded back over the landing spring, and 0 for a rubber-band pull at the strip's end. **The table is the OTHER PAGE, flat, since §898c (user: "ok lets do L and M"): `DS.swipeTable` is white under the dark card — Apple's `#f2f2f7` since §898d (user: "the white is a bit bright" → "yes do P"), the one system value the app borrows — and black under the light one, its ink `DS.swipeTableInk` the other page's, `BrandSheet` deleted, the pink retired (the day divider already speaks in the mark's hue, §740). What follows is the record of §898b.** **The table was a sheet of FROSTED GLASS over the pink in §898b** (`Design/BrandSheet.swift`: one light high and left, a fall-off, one window reflection, darker corners, 7% milk, grain; one static picture per window size, rendered off main from `.task`, the flat token standing in until it exists; the card's shadow widens to 40/24 because frost scatters light; a live `Material` was mocked and declined — behind the coat there is only the page, so glass there reads as a grey slab). **The colour under it is the mark's hue in SHADOW** — `#6b1c3e` since §898a (§898 landed `#8c2451`, B 55; stepped down to B 42 off a six-panel board, user: "ok do it", 2026-09-24), hue 334° held, S 74, a third register of the one pink after `brand` and `brandInk` (user: "it's like it's shadowed … make it more shadowed to tone it down"); full-chroma was mocked and refused as a flash on every swipe. **Nil on a vivid page or a photo**, `brandInk`'s rule, and the cover's word then takes `textPrimary`; on the ground it is `brandGroundInk` (white in BOTH themes, 11.3:1 — `textPrimary` is black on the light page and 1.9:1 here). **The cover draws NO ground and NO picture**: the bare mark (a white rim and a black rim were both mocked and read as accidental; the word beneath already frames it) and the name at the entering edge, riding the finger as before. `RoomSnapshots` and `captureRestingLook` are DELETED with their only reader (§723) — a pink table cannot also be a preview, and a `drawHierarchy` nobody reads is 20–40ms on main for nothing. **The room's OWN colour was mocked and declined**: X, ChatGPT, ether.fi and Apple Wallet are black or white, so the rule died on a third of the dock, and §524 already took source hue off every ground. `dock-selftest.sh` section 12 and `room-perf-selftest.sh` guard it. **UNSEEN on a device** — built on Linux from the mocks; the landing (the pager cuts to the room while the strips above and below fade) is the first thing to watch.

- **A ROW LEAD TURNS TO ITS DOCK CATEGORY'S GLYPH AND BACK, ONCE (prd §901, 2026-09-23 — user: "one cycle not a switch" … "always the category not a state b/c then we'd be making up new icons or glyphs for a user to learn").** `LeadCycle` (`Design/LeadCycle.swift`), one modifier on `BandRow`'s lead. Two moments, one rule, one clock: a row that LANDS while you look (captured after `feedWaveAt`, the page's `shapeWaveAt`, and within 20s of now) or whose TITLE moves in place turns its 26pt source mark over on its Y axis to `CategoryFold.glyph(for: BridgeCatalog.category(forSource:))` — the glyph the category chip wears — holds, and turns back; out at 0.3s, back at 1.2s, reset to rest with no animation. The back face is `DSGlyphLead`. The turn is one `Animatable` modifier (`Face`) deriving each face's visibility from the animated angle, so one transaction turns it out and one back — not four with a swap at the edge. **What was declined and why**: a firmer tick on "Since you left" (noisy), a pink ring on new rows (overkill under constant arrivals, and §740), stretching the floor's mark (the draw-on already marks the moment), and state glyphs (a tick for delivered is a second vocabulary). **The bounds**: the corpus you open to stays at rest (the cascade is its motion, §661); a row met by scrolling stays at rest (the fresh window); the wallet ledger's rows keep §171's ripple as their one motion on a retitle (`cyclesOnChange: !moneyColumn`); a source with no dock category has no second glyph and does not turn; Reduce Motion turns nothing. `lead-cycle-audit.py` pins all of it, sixteen mutations. `-leadCycleProbe <s>` lands a Trello thing and retitles it 3s later. **The landing is the thing's ARRIVAL, never its date (prd §901b, 2026-09-25 — user: "i don't notice it. is it live").** It was live and almost never fired: `capturedAt` is stamped from upstream by most bridges, so a forty-minute-old reply that arrived just now read as old, and the probe passed on a default `.now`. `Model/LandingLedger.swift` stamps every inserted `Thing` at its context's `willSave` (one observer, installed once from `RootShell`, bounded to 60s, suspended by the demo pour); the row passes `id: thing.id` and the cycle reads `LandingLedger.landedAt(id)` in `onAppear`. Process-local: no schema, no CloudKit, the corpus you open to still at rest. Folds still never cycle (§903). Audit rules E and G, 26 mutations.

- **THE ALL FEED'S APPLE PASS (prd §902, 2026-09-23 — user: "what would apple do to our All feed in its current state?", then five rulings, then "do we even need the time stamps?" and "do we even need to say 'N more'").** The feed was screenshotted and read before anything was drawn; four Design boards settled it. `DSFeedRow` draws ONE HEAD: a 52pt-minimum block (`headHeight`) the 26pt lead is centred in, the title on ONE line (`nameLines` deleted, 26 callers), the line on one; a row's own content still rides `below` (a post's words, a music row's tiles, a fold's art). The trailing slot keeps MONEY, a CLOCK STILL AHEAD OF YOU and LIVE, and nothing else: every `LiveTimeText` left the rows (the day header says when; the sheet has the exact time), `lineTail`/`DSFeed.more` are deleted (the fold lead's second plate says "more than one"), the chat row's message count went too, `timeInk` and §900's heavier new-time are superseded. The ONE age that stays is an alarm-class arrival's, in `DS.destructive` (`KNOWN_TIME`). The line never names the source (`sourceBadge` deleted; a watched wallet's NAME is the project clause and stays). The All feed's day divider takes `EmptyView()` — no subject line, no whisper sentence, no "Last here". The card is untouched. `feed-row-skeleton-audit.py`'s time rule is INVERTED (a row drawing an age is the finding), `feed-grammar-selftest.sh` has five new guards and six new mutations. Seen on the simulator, both themes.
- **A FOLD'S LEAD IS THE BARE MARK (prd §903, 2026-09-24 — user: "in the All feed, what are these gray tiles behind the icons", "it kind of looks like an error and won't all of them always have multiple things", "remove that tile").** `DSFoldLead` is deleted; `BundleRow` and `StripRow` lead with `BridgeIcon(name: source, size: DS.Mark.row)`, the single row's own lead. The plate read as a misrender (no other mark is drawn offset and doubled; §782 allows no plate under a mark), and on a source that lands three a day (`FeedFold.bundleThreshold`) it stood every day, which is §902's own case against "N more". A fold is now told apart by nothing in its row; a strip's tiles and a bundle's art still ride `below`, and the tap still opens the room. `feed-grammar-selftest.sh` guards the bare mark on both folds and fails on the deleted name; `lead-cycle-audit.py` rule D reads the fold structs' bodies. Unseen on the simulator (Linux host).
- **THE GRID STANDS UNDER THE DAY (prd §910, 2026-09-24 — user: "how if at all would you improve the way we lay out screenshots and files in those rooms … between them in the grids", then "ship it").** `photoGridSection` is deleted; `groupedSections`/`daySection` take `isTile:` + `tileShape:` and each day tiles its own pictures under its header, three to a `List` row (`tileRows`), its other things beneath. `PhotoCell.Shape`: `.screenshot` is 3:4 cropped from the top (`PhotoWell(anchor:)`), `.square` centred; the caption is `subhead12` UNDER the picture, none for a camera-named file (`isCameraName`); the day pill and the scrim are gone. Mockup in `design/mockups/photo-file-grids.html`; seen on the simulator, both themes.

- **Kind tiles, the glyph rule read both ways (prd §831).** Privy's Apps wore Frames' glyph for a month because the guard read the table, where one constant had one symbol, and never the conformances, where a second meaning pointed at it. `room-kind-tiles-selftest.sh` now reads the conformances too: a `DSTileScope` case wears the constant of its own name, or the pair is declared an alias with its reason, and a stale alias fails.

- **Every room opens on the box, and twelve more rooms carry kind tiles (prd §911, 2026-09-24).** The inventory found eleven rooms that could open on a bare row and twelve whose rows already carried a kind and drew no tiles. Calendar covers the NEXT event (`nextEventID` first), Reminders its newest open reminder, Gmail its newest mail with "Waiting on you" UNDER it; CardPointers, Bitrefill, Walletbeat, L2BEAT and Pinned cover when their head or lede is absent; Tokens' lede draws over one token and says "N tokens watched" before any pulse is read; `roomBody`'s final `else` draws the empty state where a devnet head is nil. `standaloneLead(cover:tiles:listEmpty:)` is the one drawing for a cover, an empty lead and tiles — a room's tiles never depend on its head existing. Polar and Dodo Payments ride Stripe's slot with FOUR tiles (All · Sales · Subscriptions · Disputes — a fifth wrapped alone onto a second row, measured; refunds are All only); GitLab, Radicle, Sentry, Vercel, PagerDuty, npm, PyPI, AWS, Cursor and Apple Health take tiles under the cover. A merge request and a patch wear the pull-request glyph as declared aliases. No attention dots on any of them. `SourceActions`' expand phrases are deleted (§723). Seen on the simulator, demo, all rooms named.

## The Apple polish pass (prd §915, 2026-09-24)

Nine live screens read as Apple would read them, and six rulings built from the ranked
canvas (https://claude.ai/artifact/4xhSVhPRyXNqB2KtCkknzt). Where each lives:

- **Say every fact once** — `SocialRoom.sentenceNamesAuthor` / `rowSentence`
  (Foundation-only, `social-room-selftest.sh`). `PostCard` draws a notice's sentence in
  the name slot when it opens with the author's handle and fits one line (72 chars, no
  newline); `FeedLedeCard.eyebrow` drops the name over such a statement. A post keeps
  the name line and its words below.
- **One scale of hierarchy** — `heading34` in `Shared/Typography.swift` is a SCREEN's
  name and nothing else (`DSScreenHead`); the Accounts switcher is `heading17`, primary
  for the pick and secondary for the rest; a section stays `heading24`.
- **One title seam** — `Model/TitleSeam.swift`: `join(object, qualifier)` clamps the
  OBJECT (ellipsis counted) so the qualifier survives the 80-character line, `split`
  reads the LAST ` — `. Drawing sites: `BandRow` (the qualifier is the line's first
  clause), `MusicRow`, `FeedLedeCard` (`words`, `qualifierLine`, `mediaWell`),
  `LiveStreamHero`, `ThingSheetView.titleBlock` and the note head, `chartParts`.
  `title-seam-audit.py` fails a `title:` / `title =` / demo `row(.kind, "…"` literal
  carrying ` · ` outside the money allowances, and any hand split of `" — "`.
  A money title's dot is `BandRow.titleMoney`'s (§900) and stays.
- **Words under art** — `FeedLedeCard.mediaWell` and `LiveStreamHero` stack the picture
  (`mediaArtHeight` = `leadHeight` − 100) over the words on a `dsWell`; no scrim.
- **The list just ends** — `caughtUpSeam` and the closing sentences are gone from
  `FeedScreen`; `caughtUpFooter` draws only the fetch-ceiling door, behind the same
  scope gates (§482, §486).
- **One notch** — `DS.fillFaint` dark `#ffffff1a` (was `0a`). The gear was already glass;
  the first-run rain keeps every tile by the 2026-09-01 ruling.

## Moved from CLAUDE.md verbatim (2026-09-24, the 100KB rule)

- **Kind tiles (prd §815, §816): Safe, GitHub, Stripe, App Store Connect, Hugging Face, PostHog, L2BEAT and Walletbeat. A room with a head KEEPS it and the tiles ride its `scopes:` slot (`DSRoomChassis.Head(... tiles:)`, the Privy geometry); a room with no head drawn leads with its cover (the full box, §904), `DSScopeTiles` `contentGap` under it, the list `leadGap` under them. All is first and where the room opens; fewer than two kinds draws no tiles; one meaning is one case and one glyph across every tile and dock seat, and the rule reads BOTH ways — **a `DSTileScope` case wears the constant of its own NAME** (`ScopeTileGlyph.apps` for `.apps`), a mismatch declared as an alias with its reason or it fails, a stale allowance failing too (`Model/RoomKindTiles.swift`, `Screens/ScopeTileGlyphs.swift`, `room-kind-tiles-selftest.sh`).** A lead with no scopes slot (a heatmap, Cloudflare's runway) gets no tiles. **Instagram, X and TikTok get NO tiles and lead with their newest thing alone — no topic map, heatmap or picture-grid lead (`SocialRoom.leadsWithNewest`); the Instagram head model is deleted** → prd §815 · §816 · §821 · §831
- **The keyboard COVERS the dock; it never lifts it (prd §865, and §865a is the half that shipped broken).** SwiftUI inflates the bottom safe area for the keyboard and every layer rises unless it opts out. **§865's one-modifier fix did NOTHING — measured on the first simulator run — because a child opting out does not undo a parent whose bounds the keyboard already compressed.** The SEAT stays put structurally: `dsStaysUnderKeyboard()` (`DSDock`) is `ignoresSafeArea(.keyboard, edges: .bottom)` UNDER a full-height bottom-aligned frame, called once, from `RootShell`. The BAND cannot use it at all — it is a `safeAreaInset` whose reserved HEIGHT is what must yield, so `bandInset` draws and reserves nothing on `ShellChrome.keyboardUp` (one flag, one reader, and `keyboardFloor` so an iPad shortcut bar is not a keyboard). The scroll view still rises, or the focused field's baseline lands under the keyboard (`dock-selftest.sh` check 10, 8 mutations). The chat box keeps its size (`lineLimit(1...6)`, already scrolls) and a custom keyboard is declined → prd §865 · §865a

## A mark packs, a word tiles (prd §917, 2026-09-25)

- **The wallet's holdings map and the source maps are CIRCLE PACKS (`Design/CirclePack.swift` + `Design/DSCirclePack.swift`): each item is one circle whose area is its share and whose mark is its label — no cell, no name, no wash, a 44pt floor instead of a fold.** The rule picking a figure: a cell with a mark of its own on one scale packs (token and `source` modes of `GenTagMap`, the brief's `SourceMix`); a word, or amounts not on one scale, keeps `UnitTreemap` (themes, OCR terms, the receipts' hosts, the devnets' holdings, the money hero's mini map). The pack speaks the treemap's beat — `settleIn` biggest first, §501's travel on the caller's ids, `DSTileButtonStyle`, the Mac readout — because that is where the cohesion between two figures lives. `circle-pack-selftest.sh` compiles the geometry with `swiftc` and proves area ∝ share, the floor, no overlap, centring and input order, five mutations deep → prd §917
