# Casberi launch thread

Source of truth for the app lists: `Casberi/Casberi/Model/BridgeCatalog.swift` (34 offers — 23 connectable today, 11 marked "Arrives with bridges"). Update this file if the catalog changes before posting.

Five posts, each doing two jobs: hook + worked example → capture paths → all apps + reply hook → privacy → why + CTA.

---

**1/**
Your stuff is spread across a dozen apps — screenshots in Photos, links in bookmarks, notes in three places, your accounts in multiple wallets, your token watchlist in another app, chats somewhere else. Keeping tabs on any of it means opening app after app.

Casberi puts it all in one feed. Ask about any of it, and the answer is built from your own stuff.

*[attach: screen recording of a real ask]*

**2/**
Links, screenshots, notes, voice memos, events, chats, tasks, workouts, wallet activity, the tokens you watch — it all lands in one feed, and you never file anything. Share to it, paste into it, talk to it, drop a file on it, or let screenshots land on their own.

Save now, ask later in plain words.

**3/**
It connects to dozens of apps. Working today: Photos, Calendar, Reminders, Apple Health, Apple Music, Gmail, iCloud Mail, ChatGPT, GitHub, Linear, Notion, Obsidian, Todoist, Cal.com, Calendly, Readwise, Raindrop, RSS, Bluesky, Farcaster, Steam, Wallet, Dexscreener.

Coming next: Claude, Venice, OpenClaw, X, Reddit, YouTube, Spotify, Telegram, Slack, Strava, Twitch. Tell me which you'd use — that decides what I build first.

*[attach: catalog screenshot]*

**4/**
The model that reads your things and builds your screens runs on the phone — Apple's on-device model, iOS 26. Sync is your own iCloud. "Delete everything" deletes everything, including the iCloud copy.

**5/**
Built alone, because I kept losing my own stuff and wanted one place that answers instead of making me search.

iPhone, iOS 26. casberi.app

---

## Posting notes

- **Post 3 is ~470 characters** — it needs X Premium. Without Premium, split it back into two posts (working today / coming next) and the thread becomes six.
- **Post 1 needs its recording before this can go out** — with the copy now leading on the pain (app-hopping), the recording is the only place the generated-screen demo lives. Pick a query the app demonstrably handles end-to-end and record that exact ask.
- **Post 5's "kept losing my own stuff" is a guess at the origin** — replace with the real reason, or cut the clause and lead with "Built alone."
- **Post 5 needs a real ask** — decide what casberi.app offers a visitor that day (waitlist, TestFlight, follow-for-launch) and put that verb in the post. The App Store link replaces it once Developer Program enrollment clears.
- **Post 3's promise is a commitment** — only keep "that decides what I build first" if replies will actually influence bridge order.
- No exact counts in the thread — "dozens" by ruling (2026-07-08), so the copy doesn't go stale as the catalog grows. The working/coming split mirrors `connectable` in the catalog — don't merge the lists; the app itself refuses to pretend shelf bridges are wired ("Arrives with bridges"). The catalog screenshot names every app even if the text list is ever trimmed.
- No end-to-end-encryption claim anywhere — that requires the user's Advanced Data Protection, per the ship-gate ruling.

## Cut from the 8-post version

- The standalone worked-example post — folded into post 1's hook and recording.
- The standalone "coming next" reply-hook post — folded into post 3.
- The full origin-story post — compressed to one clause in post 5.
