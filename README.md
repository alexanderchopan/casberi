# Casberi

- `Casberi/` — the Xcode project (iOS app + ShareExtension).
- `docs/` — product docs: PRD draft, build brief, name ledger, session handoff notes.
- `prototype/` — the v94 composer web prototype (`.tsx` source, `.html` standalone export).
- `design/app-icon/` — app icon sources (SVG masters + generator script). The exported 1024px PNGs live in the asset catalog at `Casberi/Casberi/Assets.xcassets/AppIcon.appiconset/`.
